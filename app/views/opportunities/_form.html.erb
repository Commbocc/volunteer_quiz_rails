<%= form_for(opportunity) do |f| %>
  <% if opportunity.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(opportunity.errors.count, "error") %> prohibited this opportunity from being saved:</h2>

      <ul>
      <% opportunity.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :name %>
    <%= f.text_field :name, class: 'form-control' %>
  </div>

  <div class="form-group">
    <%= f.label :link %>
    <%= f.text_field :link, class: 'form-control', placeholder: 'http://' %>
  </div>

  <div class="form-group">
    <%= f.label :image_src %>
    <%= f.text_field :image_src, class: 'form-control', placeholder: 'http://' %>
  </div>

  <div class="form-group">
    <%= f.submit nil, class: 'btn btn-default' %>
  </div>
<% end %>
